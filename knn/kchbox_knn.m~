% Perform KNN on a test data
% Input: test_d(1xf), train_d(mxf), train_label(1xm), where f is the number
% of features, m is the number of 
function prediction = kchbox_knn(k,test_d,train_d,train_label)




end




